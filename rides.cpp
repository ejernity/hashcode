#include intersection.hpp

class Rides{
	private:
		intersection start_intersection;			// position to begin ride
		intersection finish_intersection;			// position to finish ride
		int earliest_start;							// the earliest step that ride can start
		int latest_finish;							// the latest step that ride must finish to get points
	public:
	
};
